# I-Hungry-Burger-Shop-Linked-List-Edition-
This version uses a custom linked list (Node class) to manage orders dynamically in memory. Each Node contains a BurgerOrder object, enabling efficient insertion, traversal, search, and update of orders. It builds upon earlier versions with a full Swing GUI, offering a realistic mini POS simulation
